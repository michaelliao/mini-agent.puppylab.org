# Run Shell

Run shell command.

## description

Run a shell command.

## usage

{command_line}

- command_line: the command line as string, e.g. "ls -l ./build"

## author

Crypto Michael

## NOTE

Run shell command is DANGEROUS in local machine. Make sure it runs in a sandbox (e.g. virtual machine).
